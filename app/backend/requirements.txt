# The viewer needs nothing the project environment does not already have: flask and
# flask-cors are pinned in requirements-lock.txt together with torch, torchvision,
# opencv, numpy, scipy and pyyaml.  Install that one file.
-r ../../requirements-lock.txt
