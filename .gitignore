# Python
__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
build/
dist/

# Environments
.env
.env.*
.venv/
venv/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Editors and operating systems
.vscode/
.idea/
.DS_Store
*~
*.tmp
.nfs*

# Logs
*.log
logs/

# Model weights: distributed separately, never committed (see README)
*.pt
*.pth
*.ckpt
*.onnx

# Download targets and third-party assets.  weights/ holds the release checkpoints
# and the SegmentAnyTooth weight directory, inputs/ the vendored MIT source; both are
# put in place by hand and linked into third_party/ by scripts/fetch_third_party.py.
weights/
inputs/
third_party/

# Experiment artefacts: every one is regenerable from the code and the configs
results/
runs/
slurm_driver_logs/

# LaTeX build products; the sources and the figure data are tracked
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.pdf
paper/llncs.cls
paper/splncs04.bst
paper/generated/figures/**/*.pdf
paper/generated/figures/**/*.png
paper/generated/figures/**/*.jpg
paper/generated/figures/**/*.placeholder
# Rendered qualitative panels are patient photographs and are regenerable with
# `make figures`; only the numeric source data behind each figure is tracked.
paper/generated/figures/five_views/

# Viewer
app/frontend/node_modules/
app/frontend/dist/
app/backend/outputs/

# Session scratch
prompt.md
wget-log*
.claude/
